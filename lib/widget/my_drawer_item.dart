import 'package:flutter/material.dart';

class MyDrarerItem extends StatelessWidget {

  final iconPath;
  final text;
  final rightIcon;

  MyDrarerItem(this.iconPath,this.text,this.rightIcon);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Row(
        children: <Widget>[

        ],
      ),

    );
  }
}
